{
  "name": "coursesnag-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test",
    "check": "for file in src/*.mjs; do node --check \"$file\"; done"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.1102.0",
    "@aws-sdk/client-sqs": "^3.1102.0",
    "@aws-sdk/client-ssm": "^3.1102.0",
    "@aws-sdk/lib-dynamodb": "^3.1102.0"
  }
}
